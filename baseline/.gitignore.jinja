# rl3-templates: managed BEGIN
# This block is generated by rl3-templates and refreshed on every `copier update`.
# Do not hand-edit between BEGIN/END markers. Add project-specific rules below the END marker.

# ── Secrets and local environment ─────────────────────────────────────────
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
*.jks
*.keystore
id_rsa
credentials.json

# ── Operating system junk ─────────────────────────────────────────────────
.DS_Store
.AppleDouble
.LSOverride
._*
Thumbs.db
ehthumbs.db
Desktop.ini

# ── Editor and IDE ────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*.iml
.project
.classpath
.settings/

# ── Agent runtime artefacts ───────────────────────────────────────────────
.claude/
.cursor/
.cline/
.windsurf/
.codex/
.codi/
.aider*
.continue/

# ── Logs ──────────────────────────────────────────────────────────────────
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
{% if has_python %}

# ── Python ────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
*.egg
.eggs/
build/
develop-eggs/
downloads/
eggs/
parts/
sdist/
wheels/
share/python-wheels/
.installed.cfg
.Python

# ── Python virtual envs and tool caches ───────────────────────────────────
.venv/
venv/
ENV/
env/
.ruff_cache/
.mypy_cache/
.pytest_cache/
.pyright/
.tox/
.nox/
.hypothesis/

# ── Python coverage ───────────────────────────────────────────────────────
.coverage
.coverage.*
htmlcov/
coverage.xml
*.cover
*.py,cover
.cache/
nosetests.xml

# ── Backend project subdir caches ─────────────────────────────────────────
{{ backend_dir }}/.ruff_cache/
{{ backend_dir }}/.mypy_cache/
{{ backend_dir }}/.pytest_cache/
{{ backend_dir }}/.coverage
{{ backend_dir }}/htmlcov/
{% endif %}
{% if has_typescript %}

# ── Node / pnpm / yarn ────────────────────────────────────────────────────
node_modules/
.pnpm-store/
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz
.npm
.eslintcache
.stylelintcache

# ── Build output ──────────────────────────────────────────────────────────
dist/
build/
out/
.next/
.nuxt/
.svelte-kit/
.vercel/
.netlify/
.turbo/
.parcel-cache/
*.tsbuildinfo

# ── Frontend project subdir artefacts ─────────────────────────────────────
{{ frontend_dir }}/dist/
{{ frontend_dir }}/build/
{{ frontend_dir }}/.next/
{{ frontend_dir }}/.turbo/
{{ frontend_dir }}/.tanstack/
{{ frontend_dir }}/test-results/
{{ frontend_dir }}/playwright-report/
{{ frontend_dir }}/blob-report/
{{ frontend_dir }}/playwright/.cache/
{{ frontend_dir }}/coverage/

# ── Misc TypeScript ───────────────────────────────────────────────────────
*.d.ts.map
*.js.map
{% endif %}
{% if has_docker %}

# ── Docker volumes and runtime ────────────────────────────────────────────
pgdata/
mysqldata/
mongodata/
redisdata/
*.dockerignore.bak
{% endif %}
{% if has_terraform %}

# ── Terraform ─────────────────────────────────────────────────────────────
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
{% endif %}
{% if has_ansible %}

# ── Ansible ───────────────────────────────────────────────────────────────
*.retry
.vault_pass
ansible.log
{% endif %}

# ── Compressed archives (often accidental) ────────────────────────────────
*.zip
*.tar.gz
*.tgz
*.7z
*.rar

# ── Local copier artefacts ────────────────────────────────────────────────
*.rej
*.orig
.copier-answers.yml.bak

# rl3-templates: managed END
# ─────────────────────────────────────────────────────────────────────────
# Project-specific rules below — preserved across `copier update`.
# Add directories, files, or patterns specific to this project here.
# ─────────────────────────────────────────────────────────────────────────
