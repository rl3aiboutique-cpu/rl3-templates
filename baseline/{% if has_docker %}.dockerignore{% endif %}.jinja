# rl3-templates: managed BEGIN
# This block is generated by rl3-templates and refreshed on every `copier update`.
# Do not hand-edit between BEGIN/END markers. Add project-specific rules below the END marker.
#
# Goal: minimise Docker build context so `docker build` is fast and image layers
# never carry secrets, lockfile noise, or local tool caches.

# ── VCS ───────────────────────────────────────────────────────────────────
.git/
.gitignore
.gitattributes
.gitkeep

# ── CI and template machinery ─────────────────────────────────────────────
.github/
.gitlab-ci.yml
.copier-answers.yml
.pre-commit-config.yaml
.gitleaks.toml
.yamllint.yaml
.codespellrc

# ── Documentation (not needed at runtime) ─────────────────────────────────
docs/
*.md
CHANGELOG.md
README.md
LICENSE

# ── Secrets ───────────────────────────────────────────────────────────────
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
*.jks
*.keystore
id_rsa
credentials.json

# ── Editor / OS junk ──────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# ── Agent runtime ─────────────────────────────────────────────────────────
.claude/
.cursor/
.cline/
.windsurf/
.codex/
.codi/

# ── Logs ──────────────────────────────────────────────────────────────────
*.log
logs/
{% if has_python %}

# ── Python build / cache (regenerated inside the image) ───────────────────
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.ruff_cache/
.mypy_cache/
.pytest_cache/
.tox/
.coverage
.coverage.*
htmlcov/
coverage.xml
{{ backend_dir }}/.venv/
{{ backend_dir }}/.ruff_cache/
{{ backend_dir }}/.mypy_cache/
{{ backend_dir }}/.pytest_cache/
{{ backend_dir }}/htmlcov/
{% endif %}
{% if has_typescript %}

# ── Node build / cache (regenerated inside the image) ─────────────────────
node_modules/
.pnpm-store/
.yarn/cache
.npm
.eslintcache
.stylelintcache
dist/
build/
.next/
.turbo/
.parcel-cache/
*.tsbuildinfo
{{ frontend_dir }}/node_modules/
{{ frontend_dir }}/dist/
{{ frontend_dir }}/build/
{{ frontend_dir }}/.next/
{{ frontend_dir }}/.turbo/
{{ frontend_dir }}/test-results/
{{ frontend_dir }}/playwright-report/
{{ frontend_dir }}/coverage/
{% endif %}

# ── Test artefacts ────────────────────────────────────────────────────────
test-results/
playwright-report/
.nyc_output/

# ── Local copier artefacts ────────────────────────────────────────────────
*.rej
*.orig

# rl3-templates: managed END
# ─────────────────────────────────────────────────────────────────────────
# Project-specific rules below — preserved across `copier update`.
# ─────────────────────────────────────────────────────────────────────────
