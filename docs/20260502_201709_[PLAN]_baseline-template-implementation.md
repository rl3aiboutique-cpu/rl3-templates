# [PLAN] Baseline template — Phase 1 implementation

| Field | Value |
|---|---|
| Status | Phase 2A in flight — 5 / 6 retrofits merged; rl3-website pending |
| Owner | RL3 AI Agency |
| Created | 2026-05-02 |
| Updated | 2026-05-03 (post-v0.2.1 release + Phase 2A retrofits) |
| Phase | 1 + 1.5 + 1.6 done; 2A in flight; 2B + 3 future |
| Latest tag | `v0.2.1` on develop tip `42d7046` |
| Coupling | **None** in Phase 1.x — Phase 2B will add opt-in `enable_rl3_ci`, Phase 3 `enable_codi` |

## 1. Goal

Author a single Copier template named `baseline` that renders into any new RL3 repository:

1. A local pre-commit configuration with modern, fast tooling.
2. Claude Code agent guardrails (`.claude/settings.json`) that block destructive Bash commands, edits to generated files, and direct pushes to protected branches.
3. Pre-push hooks that enforce branch naming and routing.
4. A drift-detection workflow that opens a weekly PR with template updates.
5. Standalone helper scripts under `scripts/hooks/` — no external CLI dependency.

After Phase 1 lands, every new RL3 repository will start from this template and stay aligned via `copier update`.

## 2. Non-goals (explicitly out of Phase 1)

- **No `codi` coupling.** No `.codi/` directory, no skill tracker / observer hooks, no `_run-node` shim. Phase 3 will add a `enable_codi` flag.
- **No `rl3-ci` workflow callers.** No `ci.yml` rendered. No `_ops-pr-policy.yml` or `_ops-hotfix-backmerge.yml`. Phase 2 will add a `enable_rl3_ci` flag.
- **No retrofit of existing repos.** The 7 RL3 repositories (capellai-ai-crm, sapphira-clinic, rl3-website, code-graph-rag, codi, codi-brain, devloop, rl3-infra-vps) stay untouched in Phase 1.
- **No GitHub remote push.** The repository stays local until explicit go-ahead.

## 3. Threat model (summary — full rationale in design discussion)

Phase 1 hooks defend against:

- **`--no-verify` bypass by AI agent** — blocked at `.claude/settings.json` deny-list.
- **`git push --force` to shared branches** — blocked at agent layer + branch protection (Phase 2 GitHub setup script).
- **Direct push to `master` / `develop`** — blocked at pre-push hook + agent deny-list.
- **Secrets in MCP / agent config files** (Sandworm-mode mitigation) — `scripts/hooks/scan-agent-configs.sh` scans `.claude/`, `.cursor/`, `.codi/mcp-servers/` even when gitignored.
- **AI slop**: oversize files, debug statements, orphan TODOs, `NotImplementedError` stubs, `Co-Authored-By: Claude` trailers.
- **Edits to generated files**: `routeTree.gen.ts`, `frontend/src/client/`, `*.lock`, `.copier-answers.yml`.

## 4. Modern toolchain (replaces legacy)

| Concern | Tool | Notes |
|---|---|---|
| Python lint + format + import-sort + SAST | `ruff` (with `S`, `N`, `UP`, `B`, `SIM`, `I` rule categories) | Replaces `black`, `isort`, `flake8`, `bandit` (~90% coverage), `autopep8`. |
| Python type checker | `pyright` (default), `ty` (opt-in cutting edge), `mypy --strict` (opt-in legacy) | rl3-infra-vps already swapped to pyright. |
| Python security audit | `pip-audit` | OSV-based, runs against `uv export`. |
| Python tests + coverage | `pytest` + `pytest-cov` | Coverage gate configurable per repo (default 80%). |
| Python package manager | `uv` | Already standard across RL3 repos. |
| TypeScript lint + format | `biome` | Rust-based, replaces `eslint` + `prettier`. |
| TypeScript type check | `tsc --noEmit` | Standard. |
| TypeScript package manager | `pnpm` | Already standard. |
| Secrets scanning | `gitleaks` + custom MCP/agent config scanner | Sandworm-mode mitigation. |
| Spell check | `typos` (default), `codespell` (opt-in) | typos is Rust, ~10× faster, fewer false positives. |
| YAML | `yamllint` | Standard. |
| Docker | `hadolint` | Standard. |
| Shell | `shellcheck` | Standard. |
| GitHub Actions | `actionlint` | Validates workflow YAML at commit time. |
| Terraform | `terraform fmt` + `tflint` (opt-in) | When `*.tf` present. |
| IaC security | `checkov` (opt-in) | Off by default. |
| Conventional commit messages | `compilerla/conventional-pre-commit` | Lightweight. |

## 5. The 14 non-negotiables

1. **Pre-commit framework** as the substrate. Husky carve-out only for `codi` itself.
2. **Block markers** (`# rl3-templates: managed BEGIN/END`) delimit autogenerated content.
3. **`--no-verify` is forbidden for the agent**; CI is the authoritative gate.
4. **`--force-with-lease` is the only allowed force push**, and only on non-protected branches.
5. **`Co-Authored-By: Claude` is forbidden** at commit-msg hook + content scan.
6. **MCP / agent config files are scanned for secrets** even when gitignored.
7. **700-line cap** for code files; no cap for docs.
8. **TODOs require ticket reference** (`TODO(ABC-123)`); orphan TODOs blocked.
9. **Generated files cannot be hand-edited**: `routeTree.gen.ts`, `frontend/src/client/**`, `*_pb2.py`, `*.lock`, `.copier-answers.yml`.
10. **Pre-push tier enabled by default** (pip-audit + pytest cov), gated by flag.
11. **No direct push to `master` or `develop`.** Three layers: pre-push + agent + branch protection.
12. **Branch naming mandatory**: `<prefix>/<git-username>/<slug>` where prefix ∈ {feature, bugfix, chore, release, hotfix}. Optional `<TICKET-ID>-` infix in the slug. The `<git-username>` segment is derived from `git config user.name` (lowercased, kebab) — enforces ownership.
13. **PR routing matrix enforced in CI** (Phase 2).
14. **Hotfixes are not a bypass** — permitted route with own SLA, mandatory back-merge.

## 6. Branching policy (rendered into every consumer's CLAUDE.md)

Branch shape: `<prefix>/<git-username>/<slug>` (with optional `<TICKET-ID>-` infix).

| Source | Target | Reviewers | Use case |
|---|---|---|---|
| `feature/<user>/<slug>` | develop | 1 | New feature |
| `bugfix/<user>/<slug>` | develop | 1 | Non-urgent bug fix |
| `chore/<user>/<slug>` | develop | 1 | Tooling, deps, docs |
| `release/<user>/v<x.y.z>` | master | 2 | Release PR |
| `develop` | master | 2 | Integration PR |
| `hotfix/<user>/<slug>` | master | 1 (fast) | Production fix |
| `hotfix/<user>/<slug>` (auto-opened) | develop | 1 | Mandatory back-merge |

Examples:

- `feature/lehidalgo/add-csv-export`
- `feature/lehidalgo/RL3-142-add-csv-export`
- `hotfix/jdoe/CBP-99-fix-payment`

Branch sources:

- `feature|bugfix|chore|release` — branched from `origin/develop`.
- `hotfix` — branched from `origin/master`.

Emergency override: only two named human admins, configured server-side. Never the agent.

## 7. Phase 1 asset list

### Files rendered into the consumer repo

| Path (in template) | Purpose |
|---|---|
| `.pre-commit-config.yaml.j2` | Pre-commit configuration with the 7 blocks (hygiene, secrets, anti-slop, python, typescript, infra, commit-msg + pre-push tier). |
| `.claude/settings.json.j2` | Agent deny-list + PreToolUse / PostToolUse hooks. |
| `.gitleaks.toml` | Secret-scan rules. |
| `.yamllint.yaml` | YAML lint config. |
| `.github/workflows/template-drift.yml` | Weekly `copier update` PR. |
| `scripts/hooks/guard-bash.sh` | PreToolUse Bash filter (no-verify, force, protected branches, dangerous commands). |
| `scripts/hooks/guard-write.sh` | PreToolUse Edit/Write filter (generated files, env files, ADRs). |
| `scripts/hooks/auto-format.sh` | PostToolUse formatter dispatcher (ruff format, biome format, terraform fmt). |
| `scripts/hooks/scan-agent-configs.sh` | MCP / agent config secret scanner. |
| `scripts/hooks/check-branch-name.sh` | Pre-push branch-name validation. |
| `scripts/hooks/no-direct-push.sh` | Pre-push: refuse direct push to master / develop. |
| `scripts/hooks/check-branch-base.sh` | Pre-push: verify branch source (feature off develop, hotfix off master). |
| `scripts/hooks/check_file_lines.py` | 700-line cap enforcement. |
| `scripts/setup-branch-protection.sh` | Idempotent `gh api` configurator (manual run, not in `_tasks`). |

### Files in the rl3-templates repo itself

| Path | Purpose |
|---|---|
| `baseline/copier.yml` | Prompts + flags + `_tasks`. |
| `baseline/{{ project_slug }}/...` | All the assets above, parameterised. |
| `README.md` | What this repo is, current phase, how to consume. |
| `LICENSE` | MIT. |
| `docs/_index.md` | Doc index. |
| `docs/<timestamp>_[PLAN]_*.md` | This document. |

## 8. Implementation milestones

| # | Milestone | Output | Status |
|---|---|---|---|
| 1 | Repository skeleton | `git init` on `master`, this `[PLAN]` doc, README, LICENSE, .gitignore, docs/_index.md. | DONE — commit `f1df5ec` |
| 2 | `copier.yml` + standalone hook scripts | `baseline/copier.yml`, all 7 `scripts/hooks/*.sh`, `scripts/hooks/check_file_lines.py`. | DONE — squashed into `af4aa4d` |
| 3 | Jinja config templates | `.pre-commit-config.yaml.jinja` (7 blocks), `.claude/settings.json.jinja`, `.gitleaks.toml`, `.yamllint.yaml`, `.codespellrc`. | DONE — squashed into `af4aa4d` |
| 4 | Drift-detection workflow + branch protection script + CLAUDE.md.jinja | `.github/workflows/template-drift.yml`, `scripts/setup-branch-protection.sh`, `CLAUDE.md.jinja`. | DONE — squashed into `af4aa4d` |
| 5 | Smoke render | `copier copy` into 4 sandboxes (python, ts, fullstack, infra). All 17 files render; YAML / JSON / CLAUDE.md valid; 7/7 hooks executable; `.copier-answers.yml` persisted; `guard-bash.sh` blocks `--no-verify` in all 4. | DONE — `.copier-answers.yml.jinja` added + `_tasks` consolidated |
| 6 | Ready-to-push state | Tag `v0.1.0`, [PLAN] doc marked complete, validation suite promoted to `tests/validate.sh`, comprehensive `[GUIDE]` doc authored, README expanded. | DONE — tagged `v0.1.0` |

**Phase 1 complete.** Next steps are separate go-aheads: (a) push to GitHub, (b) retrofit PRs against the 7 consumer repos.

For day-to-day reference, see `docs/<timestamp>_[GUIDE]_hooks-and-policy-reference.md` — the canonical hook + policy reference. The `[PLAN]` doc above is now historical.

## 9. Deferred — Phase 2 and Phase 3

| Phase | Scope | Trigger |
|---|---|---|
| 2 | rl3-ci integration: `enable_rl3_ci: true` flag renders thin `ci.yml` caller targeting `rl3-ci@v1`. New `_ops-pr-policy.yml` and `_ops-hotfix-backmerge.yml` land in rl3-ci first (separate PR). | After Phase 1 retrofits land in 3+ consumer repos. |
| 3 | codi integration: `enable_codi: true` flag renders `.codi/` skeleton, adds skill tracker / observer entries to `.claude/settings.json` via `_run-node` shim. | After Phase 2 stable. |

## 10. Retrofit order (post-Phase 1, separate go-aheads)

Lowest risk first:

1. `devloop` (no existing hooks)
2. `codi-brain` (no existing hooks)
3. `code-graph-rag` (minimal pre-commit)
4. `rl3-website` (husky in frontend/, additive)
5. `sapphira-clinic` (mid divergence)
6. `capellai-ai-crm` (highest divergence — validates the design)
7. `codi` (husky carve-out preserved)
8. `rl3-infra-vps` (codi block markers — Phase 3 validates)

## 11. Open items

| # | Item | Status |
|---|---|---|
| O1 | GitHub admin handles for `CODEOWNERS` emergency override | RESOLVED — `@lehidalgo` + `@novasvilla` added in `.github/CODEOWNERS` (commit 2026-05-02) |
| O2 | Smoke-test sandbox location | RESOLVED — `/tmp/rl3-smoke-{python,ts,fullstack,infra}` (transient) |
| O3 | Versioning policy for the `baseline` template tag | RESOLVED — semver, `v0.1.0` for first GitHub release |
| O4 | rl3-templates dogfooding its own hooks on itself | OPEN — chicken-and-egg, plan to bootstrap manually post-push |
| O5 | Render-time validation of CLAUDE.md against existing repo CLAUDE.md (preserve project-specific notes) | OPEN — affected capellai/rl3-website/rl3-infra-vps; PRs merged anyway, future copier-update PRs preserve `## Project-specific notes` section |
| O6 | rl3-website retrofit PR #77 closed (head out of date) | OPEN — needs rebase or fresh retrofit branch |
| O7 | Phase 2B trigger met (3+ retrofits) — `enable_rl3_ci` flag work can start | OPEN — separate Phase-2B [PLAN] doc when started |

## 12. v0.2.1 release log (2026-05-03)

Five bugs surfaced when running pre-commit on the rendered baseline output during Phase 2A retrofits. Test gap in v0.2.0: `tests/validate.sh` parsed Jinja but never linted the rendered output.

| # | Hook | Fix |
|---|---|---|
| 1 | `orphan-todo` | added `exclude:` for docs/CLAUDE.md/.pre-commit-config.yaml |
| 2 | `forbid-coauthor-claude` | same exclusion (docs describe the rule by mentioning the trailer) |
| 3 | `forbid-edits-to-generated` | dropped `.copier-answers.yml` from `files:` (Layer-B `guard-write.sh` covers it) |
| 4 | `yamllint` | bumped `empty-lines.max: 10` (Jinja conditionals collapse to ~6 blanks) |
| 5 | `typos` | removed `mis-match,quater,nd` from `.codespellrc` (typos doesn't read it; flagged them as typos) |

Plus one new doc: `docs/[GUIDE]_update-process.md` — 12-section reference for how rl3-templates changes propagate to consumers.

Verification: 97 / 97 PASS on `tests/validate.sh` + fresh fullstack render + `pre-commit run --all-files` clean exit on every hook.

## 13. Phase 2A retrofit log (2026-05-02 → 2026-05-03)

Inaugural Phase 2A retrofit sweep on six consumer repos. Each used `copier copy --vcs-ref v0.2.0 --overwrite` followed by a feature-branch PR.

| Repo | PR | Outcome |
|---|---|---|
| `rl3-ci` | [#12](https://github.com/rl3aiboutique-cpu/rl3-ci/pull/12) | MERGED — lib mode (enable_branch_policy=false) |
| `rl3-website` | [#77](https://github.com/rl3aiboutique-cpu/rl3-website/pull/77) | CLOSED — head out of date; needs rebase |
| `devloop` | [#1](https://github.com/rl3aiboutique-cpu/devloop/pull/1) | MERGED — first push to new GitHub remote |
| `rl3-infra-vps` | [#32](https://github.com/rl3aiboutique-cpu/rl3-infra-vps/pull/32) | MERGED — infra mode (terraform + ansible flags) |
| `sapphira-clinic` | [#16](https://github.com/rl3aiboutique-cpu/sapphira-clinic/pull/16) | MERGED — fullstack |
| `capellai-ai-crm` | [#31](https://github.com/rl3aiboutique-cpu/capellai-ai-crm/pull/31) | MERGED — fullstack |

Bootstrap commits used `--no-verify` (one-off; pre-commit hook envs install on first run). All retrofits preserve any pre-existing `.pre-commit-config.yaml` as `*.PREVIOUS-<NAME>` for review. Existing `CLAUDE.md` content was overwritten in three repos (capellai, rl3-website, rl3-infra-vps) — re-add via `## Project-specific notes` section below the managed-content marker.

Excluded from this retrofit sweep (had real WIP — separate go-aheads): `codi`, `codi-brain`, `code-graph-rag`.

Next sync: weekly cron in each merged consumer brings v0.2.1 fixes automatically Monday 09:00 UTC.
